<Page
    x:Class="Lab8_task21.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lab8_task21"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <StackPanel>
            <!-- Поля для введення даних -->
            <TextBox x:Name="Назва" PlaceholderText="Назва страви" Text="{Binding НазваСтрави, Mode=TwoWay}"/>
            <TextBox x:Name="Категорія" PlaceholderText="Категорія" Text="{Binding Категорія, Mode=TwoWay}"/>
            <TextBox x:Name="Інгредієнти" PlaceholderText="Інгредієнти" Text="{Binding Інгредієнти, Mode=TwoWay}"/>
            <TextBox x:Name="Рейтинг" PlaceholderText="Рейтинг" Text="{Binding Рейтинг, Mode=TwoWay}"/>

            <!-- Кнопки для взаємодії -->
            <StackPanel Orientation="Horizontal">
                <Button Content="Додати рецепт" Command="{Binding AddRecipeCommand}"/>
                <Button Content="Сортувати" Command="{Binding SortRecipesCommand}"/>
                <Button Content="Очистити" Click="ClearFields"/>
            </StackPanel>

            <!-- Поле пошуку -->
            <TextBox PlaceholderText="Пошук..." Text="{Binding SearchText, Mode=TwoWay}"/>

            <!-- Список рецептів -->
            <ListView ItemsSource="{Binding Recipes}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Width="200"/>
                            <TextBlock Text="{Binding Category}" Width="150"/>
                            <TextBlock Text="{Binding Ingredients}" Width="250"/>
                            <TextBlock Text="{Binding Rating}" Width="50"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>

